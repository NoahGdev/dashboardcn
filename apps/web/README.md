# web

The dashboardcn showcase site. It also serves the registry: every item in
`registry.json` is built by `shadcn build` into `public/r/<name>.json`, which
is what `npx shadcn add <url>` consumes.

See the root [README](../../README.md) for the full workflow.
